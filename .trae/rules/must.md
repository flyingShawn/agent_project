若要删除文件，放到任务最后阶段再执行命令。

若修改、添加配置文件变量或库，请注意docker配置中是否进行了相对应的同步修改。

若启动前后端测试，端口被占用，你可以杀掉之前对应所在的前端端口或后端8000进程再开始。

代码修改完成后，使用你git-commit-manager skill中的规则

logger.info或logger.warn等内容前，先添加有个换行符\n，比如logger.warning(f"\n向量化失败: {e}，使用随机向量回退")
