# 快速测试指南

## 测试目标

验证阳途智能助手的核心功能是否正常运行。

---

## 前置条件

| 服务 | 端口 | 检查方式 |
|------|------|----------|
| Ollama | 11434 | `ollama list` |
| Qdrant | 6333 | 浏览器访问 `http://localhost:6333/dashboard` |
| MySQL/PostgreSQL | 3306/5432 | `python scripts/测试数据库连接.py` |

---

## 启动步骤

### 1. 启动后端

```bash
uvicorn agent_backend.main:app --reload --host 0.0.0.0 --port 8000
```

验证：浏览器访问 `http://localhost:8000/api/v1/health`，应返回 `{"status":"ok"}`

### 2. 启动前端

```bash
cd agent_frontend
npm install   # 首次需要
npm run dev
```

验证：浏览器访问 `http://localhost:5173`

---

## 功能测试

### 健康检查

```bash
python -c "import urllib.request; print(urllib.request.urlopen('http://localhost:8000/api/v1/health').read().decode())"
```

### API 文档

浏览器访问 `http://localhost:8000/docs`

### SQL 查询测试

在前端输入：
- "当前有多少部门"
- "部门下机器数量最多的前十部门"

### 图表生成测试

在前端输入：
- "部门下机器数量最多的前十部门，以及折线图"

---

## 常见问题

| 问题 | 解决方案 |
|------|----------|
| 端口 8000 被占用 | `Get-NetTCPConnection -LocalPort 8000 \| Select-Object -ExpandProperty OwningProcess \| ForEach-Object { Stop-Process -Id $_ -Force }` |
| Ollama 连接失败 | 检查 `ollama list` 是否有模型，运行 `ollama serve` |
| 模型不存在 | `ollama pull qwen3.5:9b` |
| 数据库连接失败 | 检查 `.env` 中的 `DB_HOST`/`DB_PORT`/`DB_NAME`/`DB_USER`/`DB_PASSWORD` |
| npm 命令不存在 | 安装 Node.js：https://nodejs.org/ |
| PowerShell 脚本限制 | `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass` |

---

## 相关文档

- [配置文件说明.md](配置文件说明.md)
- [数据库配置指南.md](数据库配置指南.md)
- [Docker安装指南.md](Docker安装指南.md)
