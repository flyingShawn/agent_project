FROM agent-docling-sync-base:latest

WORKDIR /app

COPY agent_backend ./agent_backend
COPY scripts ./scripts

CMD ["sh", "-c", "python scripts/sync_rag.py --target ${SYNC_TARGET:-all} --mode ${SYNC_MODE:-incremental}"]
